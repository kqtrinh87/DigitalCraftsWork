// 1. In JavaScript, print numbers from 1 to 10. 

for (let i = 1; i < 11; i++) {
// For Loop (Let's start at one, as long it's less than 11, increment by one)
    console.log(i);
    // Print the number (pointing at) whichever the index is in the loop
}