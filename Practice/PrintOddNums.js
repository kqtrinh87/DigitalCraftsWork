// 2. In JavaScript, print the odd numbers less than 100. 

for (let i=1; i < 100; i+=2) {
    // For Loop (Let's start at one, as long it's less than 100, increment by two)
    console.log(i);
    // Print the number (pointing at) whichever the index is in the loop
}